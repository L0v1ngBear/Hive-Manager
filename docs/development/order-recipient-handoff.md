# 订单收件人先行版本（2026-09-19）

分支：`codex/feat-order-recipient`。基线：`9f8fb14`，独立工作区 `D:\HiveManager-order-recipient`。

## 范围与兼容边界

- 订单新建/完整编辑保存 `recipientName`（最多 100 字符）和 `recipientPhoneSuffix`（空或 4 位 ASCII 数字，字符串保留前导零）。详情返回两字段。
- 老客户端省略字段时保留原值；新客户端传空字符串时清空。不改客户联系电话，不自动推断收件人。
- 物流查询优先使用订单保存的尾号；旧请求参数仅在未保存尾号时兼容。顺丰缺少尾号时提示到订单编辑补充。
- 列表只显示物流信息；仅详情悬浮自动查询，不再临时要求输入尾号。保留权限、租户隔离、错误冷却和缓存；尾号变更使前端缓存键变化。
- 不涉及拆分需求任务的订单结构、状态流、审批、售后、小程序或发布流程改造。

## 后续拆分任务接入

先接入本分支，再迁移订单编辑与详情结构；保留上述字段、保存/回填、详情悬浮和测试。
迁移 `V20260919_001_order_recipient.sql` 仅追加两列，并已注册清单与校验和；上线必须先执行迁移，再启动新版后端。不得重复创建或覆盖迁移。

## 验证与发布限制

- 后端全量回归 480 项通过；随后新增内容变更、保存/省略/清空测试，针对性回归通过。
- 物流前端 11 项通过，改动文件 ESLint 通过；前端生产构建通过。
- 前端全量首次完整环境运行：464 通过、2 跳过、1 失败。唯一失败为 deploy/publish.sh 的 CRLF；仅将本地换行恢复 LF 后，对应 6 项复测通过，脚本无 Git 内容差异。
- 主分支锁文件缺少依赖项，npm ci 无法运行；本地构建临时复用原工作区 node_modules（目录链接），未修改依赖清单。此构建不是可复现发布验证。
- 尚未进行浏览器端交互验收、真实物流供应商联调或数据库迁移实跑。
- **不得直接覆盖桌面部署包。** 现有包包含权限/顶栏热修，交接文档还指出线上前端含未提交内容。须先确认可保留现有线上功能的源码发布基线，再完整测试、构建并按固定交付约定打包。本分支尚未合并主分支，未远程发布。
