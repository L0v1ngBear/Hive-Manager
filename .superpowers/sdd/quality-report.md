# Quality Management Density Report

Status: DONE

Updated `management-ui/src/views/function/badProduct/badProduct.vue` only for the approved quality-management density work:

- Added compact, responsive scope and four-statistic grids.
- Grouped the three existing date filters into one responsive date row.
- Kept the filter actions directly above the table and reduced request-state, table, and pagination spacing.
- Preserved all existing API calls, permissions, query fields, dialogs, table columns, loading, error, empty states, and action handlers.

Tests were not run, as explicitly waived in the task instructions.
